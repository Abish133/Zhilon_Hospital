# ============================================================
# RAILWAY ENVIRONMENT VARIABLES
# Copy and paste these into Railway Dashboard
# ============================================================

# ============================================================
# BACKEND SERVICE VARIABLES
# ============================================================
# Go to: Backend Service → Variables → Add Variables

NODE_ENV=production
PORT=5000

# Database (Railway auto-fills these with ${{MySQL.VARIABLE}})
DB_HOST=${{MySQL.MYSQLHOST}}
DB_PORT=${{MySQL.MYSQLPORT}}
DB_NAME=${{MySQL.MYSQLDATABASE}}
DB_USER=${{MySQL.MYSQLUSER}}
DB_PASS=${{MySQL.MYSQLPASSWORD}}
DB_DIALECT=mysql

# Connection Pool
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_ACQUIRE=60000
DB_POOL_IDLE=10000

# Authentication (CHANGE JWT_SECRET to a random string!)
JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_12345678
JWT_EXPIRES_IN=8h

# CORS (Update after frontend deployment)
CORS_ORIGIN=*

# File Uploads
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=500

# ============================================================
# FRONTEND SERVICE VARIABLES
# ============================================================
# Go to: Frontend Service → Variables → Add Variables
# Replace YOUR_BACKEND_URL with actual backend URL from Railway

VITE_API_BASE_URL=https://YOUR_BACKEND_URL.up.railway.app/api

# ============================================================
# AFTER FRONTEND DEPLOYMENT
# ============================================================
# Update Backend CORS_ORIGIN with frontend URL:

CORS_ORIGIN=https://YOUR_FRONTEND_URL.up.railway.app

# ============================================================
# NOTES
# ============================================================
# 
# 1. MySQL variables use Railway's reference syntax: ${{MySQL.VARIABLE}}
#    Railway automatically replaces these with actual values
#
# 2. Generate a secure JWT_SECRET:
#    - Windows: Use online generator or random string
#    - Linux/Mac: openssl rand -hex 32
#
# 3. Update CORS_ORIGIN after getting frontend URL
#
# 4. All variables are case-sensitive
#
# 5. No quotes needed around values in Railway dashboard
#
# ============================================================
